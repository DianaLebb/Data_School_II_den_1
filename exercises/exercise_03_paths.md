# Exercise 3: File Paths

Write the **relative path** from the workshop root to each file, then verify with `ls`.

## Tasks

1. Path to `toy_1.fastq.gz`
2. Path to `samples.tsv`
3. Path to `proteins.fa`

## Verify

```bash
ls PATH_FOR_TOY_1
ls PATH_FOR_SAMPLES_TSV
ls PATH_FOR_PROTEINS
```

## Think about it

What does `data/raw/toy_1.fastq.gz` mean in plain English?  
Why is the `.gz` extension important?
